import { initializeApp } from "firebase/app"; import { getFirestore, collection, getDocs, limit } from "firebase/firestore"; const firebaseConfig = { apiKey: "AIzaSyCxHR3QW2PfjIPvVDf84gUGzMA4MlGhECA", authDomain: "smart-attendance-system-37fd4.firebaseapp.com", projectId: "smart-attendance-system-37fd4" }; const app = initializeApp(firebaseConfig); const db = getFirestore(app); async function run() { console.log("?? Scanning users collection IDs..."); const snap = await getDocs(collection(db, "users")); if (snap.empty) { console.log("? No user documents found."); process.exit(0); } snap.forEach(doc => { console.log(`?? Document ID: ${doc.id} | Name: ${doc.data().name || "No Name"} | Role: ${doc.data().role}`); }); process.exit(0); } run().catch(console.error);
