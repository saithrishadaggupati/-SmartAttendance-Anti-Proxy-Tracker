import { getAuth, createUserWithEmailAndPassword } from "firebase/auth"; import { app } from "./src/services/firebase.ts"; const auth = getAuth(app); async function run() { console.log("?? Creating Firebase Auth record for Ananya via Client Config..."); const userCredential = await createUserWithEmailAndPassword(auth, "ananya@university.edu", "password123"); console.log("? Registration Complete!"); console.log("?? Account Created:", userCredential.user.email); } run().catch(err => { if (err.code === "auth/email-already-in-use") { console.log("?? This email is already registered in Firebase Authentication!"); } else { console.error("? Registration Error:", err.message); } });
