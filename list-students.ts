import { collection, getDocs, query, where } from "firebase/firestore"; import { db } from "./src/services/firebase.ts"; async function run() { console.log("--- CURRENT STUDENT ROSTER ---"); const q = query(collection(db, "users"), where("role", "==", "student")); const snap = await getDocs(q); snap.forEach(doc => { console.log(`ID: ${doc.id} | Name: ${doc.data().name} | ID Card: ${doc.data().studentId || "N/A"}`); }); } run().catch(console.error);
